import { SectorNamePipe } from './sector-name.pipe';

describe('SectorNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SectorNamePipe();
    expect(pipe).toBeTruthy();
  });
});
